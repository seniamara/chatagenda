import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const primary = Color(0xFF7145E8); // Roxo principal
  static const secondary = Color(0xFF45B7E8); // Azul complementar
  
  // Modo Escuro
  static const darkBackground = Color.fromARGB(255, 4, 8, 27);
  static const darkSurface = Color(0xFF1E1E2E);
  static const darkText = Color(0xFFFFFFFF);
  
  // Modo Claro
  static const lightBackground = Color(0xFFF5F5F5);
  static const lightSurface = Color(0xFFFFFFFF);
  static const lightText = Color(0xFF1A1A1A);
  
  // Cores de apoio
  static const gray = Color(0xFFCFCFCF);
  static const lightGray = Color(0xFF8E8E93);
  static const error = Color(0xFFE84545);
  static const success = Color(0xFF45E88C);
}